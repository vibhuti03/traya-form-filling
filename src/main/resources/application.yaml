spring:
  application:
    name: form-filling

  datasource:
    url: jdbc:mysql://localhost:3306/traya
    username: root
    password: lilly123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update